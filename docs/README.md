# Workspace Docs

Documentazione di livello workspace per `webapps/`.

## Contenuto

- `DEPLOY_UBUNTU_SERVER.md`: stato del deploy su server Ubuntu, servizi attivi e comandi di deploy

## Regola pratica

Qui vanno:

- runbook server;
- note di deploy;
- documentazione infrastrutturale condivisa;
- promemoria operativi che non appartengono a un singolo modulo.
